"use client";

import { PageStub } from "@/components/page-stub";

export default function MyListingsStub() {
  return <PageStub badge="Phase 3" title="My listings" subtitle="Manage your own listings (private sellers + agency members)." />;
}
